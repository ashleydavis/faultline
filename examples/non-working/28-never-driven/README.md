# A branch nothing reached

The branch is annotated, so it has a name of its own, but the phrase it compares against is put together at compile time from two halves, so no literal in the file equals it and nothing the run made up ever matched. The checklist says to write a scenario, and names the annotation.

Returns `error.SimCoveragePathsUnticked`.
