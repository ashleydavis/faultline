# A replay that fails

The same failure as `11-seed-scenario-fails`, reproduced on its own with `--replay "seed=1"`. A replay runs a strict subset of the ordinary run: the seed the plan names, and nothing else.

Prints `sim: replay of "<plan>" failed in <directory>: <error>`.
