# An invariant that does not hold

The credit is what keeps the total positive, so the run that fails that one point and then takes the amount anyway leaves the ledger negative. The invariant registered beside the scenarios catches it, and the plan that produced it is printed.

Returns the invariant's own error, which `exploreAll` treats exactly as it treats the run itself failing.
