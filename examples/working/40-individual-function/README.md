# An individual test over one function

`--function <name>` exercises that function and no other, and counts only its paths. The file here holds three functions, and the run reports one.

What it is for: a function you are working on, checked on its own without waiting for a complete test run.
