# Coverage from kcov, with no annotation anywhere

No function in this file takes a `Log` or calls `annotate`. Every branch has a line of its own, kcov reports which lines ran, and every path is ticked from that alone.

Exits 0.
