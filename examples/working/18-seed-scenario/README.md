# A scenario exercised once per seed

A seed scenario is found by its signature: the run that seed built. It is called once for each seed the package sweeps, and this package names its own seeds rather than taking the shared sweep.
