# A scenario for a path nothing else reaches

Taking a lock twice needs two calls in an order, which no single call built from a signature can produce. A scenario exercises it, and is found by its signature rather than by its name.
