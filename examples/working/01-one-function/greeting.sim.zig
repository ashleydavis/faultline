// Nothing is written here. The run calls every function from its own types, so a module whose
// functions can all be built from their signatures needs no scenario of its own.

const sim = @import("sim");
