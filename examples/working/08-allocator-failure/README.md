# An allocator that runs out

A function that allocates, with the branch it takes when there is no room reached by the failing allocator the run supplies on one call in four. The example registers nothing to make this happen.
