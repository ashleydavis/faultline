# A nested function and a private one

A function declared inside another, which gets its own checklist rather than the outer one's, and a private function reached only through its file's public caller.
