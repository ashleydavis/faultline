# A network that refuses

A function that connects, with the failing side reached through the simulated network every call is given. The example writes nothing to make this happen, which is the point of it.
