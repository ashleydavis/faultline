# A clock that does not move on its own

A function whose answer depends on time, reached with the clock standing still, jumping forward and jumping backward past where it started. The clock is a value the run builds from its type, so no scenario is written for any of the three.
