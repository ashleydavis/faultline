Fault testing the Zig source in 06-unobservable.

Deterministic simulation of 1 package. The report comes at the end.
  Read 2 source files, with 4 functions to exercise.
  Round 1: exercising 4 functions under kcov.
  .: sweeping 32 seeds.
  Round 1 ticked 6 paths; 0 functions still have a path to reach.
  Reading what those calls reached.

Deterministic simulation


  4 functions covered every path, and are not listed above.

  Found 1 source file, tested 1, exercised 4 functions and executed 6 of 6 paths.
  Every call ran against a network that refuses every connection, one call in 4 against an allocator that runs out, and one in 4 against a writer with almost no room.
  Swept 32 seeds, with no crash and every recovery invariant holding.
  Ran 1 round under kcov.
  Full detail is in <cache>/sim-coverage-report.txt.

  5 branches are not counted above, because no line, no annotation and no count can prove
  them: a `try`, `and` or `or` has no line of its own, and the false side of an `if` whose
  true side carries on has no line the true side does not share.


  Coverage: 6 of 6 paths, 100%.

  Passed: every code path ran.
