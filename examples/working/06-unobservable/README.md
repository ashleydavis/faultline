# Branches nothing can observe

`and`, `or` and `try` are branches with nowhere to put an annotation, because there is no statement position inside an expression. The run stays green, counts them, and reports them on a line of their own rather than on the checklist or in the percentage.
