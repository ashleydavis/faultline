# A factory whose state the run fills in

A factory whose state has fields built from their own types, so one factory hands back a different value on every call and nothing here says which values to use.
