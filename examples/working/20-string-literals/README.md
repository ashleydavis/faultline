# A branch that turns on a string the module declares

A comparison against `"OK"` is reached because the run reads the strings a module is written in terms of and tries them as arguments. Nothing here registers the value anywhere.
