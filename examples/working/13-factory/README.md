# A value the run cannot build on its own

A type holding a function pointer, which has no value a signature decides, reached through a factory beside the module that declares it. A factory takes a pointer to the state it is wired to and returns the type wanted, and the state is built from its own type like any other value.
