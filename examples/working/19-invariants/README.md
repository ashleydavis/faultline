# Invariants

Two things that have to hold however a write failed: a cheap one, checked after every injected run, and an expensive one, sampled. Both are registered by naming them beside the scenarios.
