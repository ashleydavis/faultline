# A project that says where its code is

This project's code is under `lib/text`, which the default arrangement does not match. `--source lib/text` names it, and that turns detection off: a project that wants to decide is never second-guessed.
