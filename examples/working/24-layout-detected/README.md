# A project whose layout finds itself

This project's code is under `source`, which the default arrangement does not match, and nothing is passed on the command line. The directories holding the code are worked out from the tree, and the run says which ones it chose before it exercises them.
